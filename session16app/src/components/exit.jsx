import React from 'react';

const exit = ({ exitGame }) => (
  <div className="center">
    <h1>Exit</h1>
    
    <button className="exit" onClick={exit}>Exit</button>
  </div>
);

export default exit;